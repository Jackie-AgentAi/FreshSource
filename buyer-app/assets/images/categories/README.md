本目录用于买家端分类默认图。

使用规则：

- 后端返回 `categories.icon` 时，前端优先使用后端图片。
- 后端未返回图片时，按分类名称匹配本目录默认图。
- 若名称未命中默认图，页面再退回通用图标占位。

当前默认图：

- `vegetable.png`
- `pork.png`
- `fish.png`
- `other.png`

后续替换正式素材时，优先保持文件名不变，这样前端代码无需再调整。
